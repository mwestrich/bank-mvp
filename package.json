{
  "name": "bank-mvp-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "postinstall": "npm run postinstall --workspace=backend"
  }
}
